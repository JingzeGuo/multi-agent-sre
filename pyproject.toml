[project]
name = "multi-agent-sre"
version = "0.4.0"
description = "Safe collaborative multi-agent SRE under partial observability"
requires-python = ">=3.10"
dependencies = [
    "langchain-core>=0.3,<2",
    "langchain-openai>=0.2,<2",
    "langgraph>=0.2,<2",
    "python-dotenv>=1,<2",
    "requests>=2.32,<3",
]
